<?php
function findInner(array $filter, array $options)
{
    $message = sprintf(
        'Instantiating aggregator %s (%d) in country %s (%d) on flow %s (%d) '
            . "at version %s with unsupported operator %s (%d)\n%s",
        $this->getAggregatorClass($id)
    );
}

error_log(
    $a
        . $b,
    3
);
call_smth();

unset(
    $action_row_copy['action_id'],
    $action_row_copy['action_ids'],
    $action_row_copy['action_ts'],
    $action_row_copy['created'],
    $action_row_copy['updated'],
    $previous_action_row_copy['action_id'],
    $previous_action_row_copy['action_ids'],
    $previous_action_row_copy['action_ts'],
    $previous_action_row_copy['created'],
    $previous_action_row_copy['updated']
);

do_something($arg1, $arg2, $arg3);
do_something(
    $arg1
);
do_other_thing(
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3
);

do_other_thing(
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3,
    $argument1,
    $argument2,
    $argument3
);
